# PUBLIC interface variables exposed to client code must be prefixed with PUBLIC_
# Set this to the base URL of the notes_database backend (e.g., http://localhost:8080 or https://api.example.com)
PUBLIC_NOTES_API_BASE=http://localhost:8080
